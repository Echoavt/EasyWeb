{
  "name": "easyweb",
  "version": "1.0.0",
  "description": "Simple static website with a small platform game",
  "scripts": {
    "start": "npx http-server -c-1 -p 8080",
    "test": "echo \"No tests specified\" && exit 0"
  },
  "devDependencies": {
    "http-server": "^14.1.1"
  }
}
